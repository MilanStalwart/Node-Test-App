<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Pagination</title>
    <style>
      body,*{padding: 0;margin: 0;background: aliceblue;font-family:"Inter",-apple-system,BlinkMacSystemFont,"San Francisco","Segoe UI",Roboto,"Helvetica Neue",sans-serif;}
      .container{max-width: 1200px;display: block;width: 100%;padding: 15px 0;margin: 0 auto;}
      .main-wrapper{display: flex;justify-content: center;flex-direction: column;gap: 20px;text-align: center;background-color: aliceblue;padding: 20px;}
      .product-details {text-align: left;display: flex;gap: 20px;align-items: center;padding: 20px;border: 5px solid #d3e3fd;font-size: 19px;}
      .pagination {display: flex;width: 100%;text-align: center;padding: 20px 0;gap: 15px;align-items: center;justify-content: center;font-size: 22px;}
      .pagination a { background: #d3e3fd; padding: 5px 20px; border-radius: 25px; color: #000; text-decoration: none; border: 1px solid #000; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="main-wrapper">
        <div class="header">
          <h1>Products Pagination</h1>
        </div>
        <% for (const item of paginatedData[currentPage - 1]) { %>
          <div class="product-details">
            <div>
              <img width="100" height="110" src="<%= item.image.src %>" alt="<%= item.handle %>">
            </div>
            <div class="product-info">
              <h3><%=item.title %> - <%=item.id %></h3>
              <p>$<%=item.variants[0].price %></p>
            </div>
          </div>
        <% } %>
      </div>
      <div class="pagination">
        <% if (currentPage > 1) { %>
          <a href="?page=<%= currentPage - 1 %>">Prev</a>
        <% } %>
          
        Page <%= currentPage %> of <%= totalPages %>
          
        <% if (currentPage < totalPages) { %>
          <a href="?page=<%= currentPage + 1 %>">Next</a>
        <% } %>
      </div>
    </div>
  </body>
</html>